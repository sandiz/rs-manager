patch-package
--- a/node_modules/line-by-line/line-by-line.js
+++ b/node_modules/line-by-line/line-by-line.js
@@ -15,13 +15,13 @@ var fs = require('fs');
 var events = require("events");
 
 // let's make sure we have a setImmediate function (node.js <0.10)
-if (typeof global.setImmediate == 'undefined') { setImmediate = process.nextTick;}
+if (typeof global.setImmediate == 'undefined') { setImmediate = process.nextTick; }
 
 var LineByLineReader = function (filepath, options) {
 	var self = this;
 
 	this._encoding = options && options.encoding || 'utf8';
-	if (filepath instanceof stream.Readable) {
+	if (filepath instanceof stream.Readable || options && options.stream) {
 		this._readStream = filepath;
 	}
 	else {
@@ -151,7 +151,7 @@ LineByLineReader.prototype.resume = function () {
 };
 
 LineByLineReader.prototype.end = function () {
-	if (!this._ended){
+	if (!this._ended) {
 		this._ended = true;
 		this.emit('end');
 	}
