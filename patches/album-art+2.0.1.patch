patch-package
--- a/node_modules/album-art/index.js
+++ b/node_modules/album-art/index.js
@@ -2,12 +2,7 @@
 
 ( ( root, cx ) => {
 
-	if ( typeof define === 'function' && define.amd ) {
-
-		// AMD
-		define( ['fetch'], cx )
-
-	} else if ( typeof exports === 'object' ) {
+	if ( typeof exports === 'object' ) {
 
 		// Node, CommonJS-like
 		module.exports = cx( require( 'node-fetch' ) )
